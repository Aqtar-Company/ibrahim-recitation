# دليل الدمج — لمبرمجٍ يضيف هذه التلاوة إلى تطبيقه

كلُّ ما يلزم لتشغيل تلاوة الشيخ إبراهيم داخل تطبيق مصحف، بطريقين: الأول
بلا معالجةٍ مسبقة، والثاني بملفٍ لكل آية.

الأمثلة كلها بقيمٍ حقيقيةٍ من البيانات، لا موضوعة.

---

## ١. اختر الطريق

| | **أ) بملف الوجه + إزاحة** | **ب) بملفٍ لكل آية** |
|---|---|---|
| التهيئة | لا شيء | قصٌّ لمرّة، ~6236 ملفًا |
| التخزين | لا شيء عندك | ~1 غيغابايت |
| تشغيل آيةٍ بعينها | اقفز إلى ثانيةٍ في الملف | افتح ملفها |
| تشغيلٌ متّصل لصفحة | طبيعيّ | يحتاج وصلًا بين الملفات |
| آيةٌ بلا توقيت | لا تُشغَّل وحدها | لا ملفَ لها |
| يشبه | مشغّل كتابٍ صوتيّ | العفاسي والحصري كما تُوزَّع |

**اختر (أ)** إن كان تطبيقك يُتابع صفحةً ويُظلّل آياتِها.
**اختر (ب)** إن كان يتعامل مع القرّاء بملفٍ لكل آية أصلًا — عندها تصير هذه
التلاوة قارئًا كسائرهم، ولا يتغيّر في كودك شيء.

---

## ٢. الطريق (أ): ملف الوجه + إزاحة

### روابط الصوت

```
https://ibrahimquran.com/quran/khatma/{page}.mp3       وجهٌ من 604
https://ibrahimquran.com/quran/surah/{n} {Name}.mp3    سورةٌ كاملة
https://ibrahimquran.com/quran/pages/{s} Page {k}.mp3  وجهٌ داخل سورته
```

التسميات فيها مسافات — **رمِّزها** (`%20`) ولا تُرسلها كما هي.
والخادم يدعم `Range`، فالقفز داخل الملف بلا تنزيله كلِّه ممكن.

### شكل التوقيتات

`data/ayah-timings.json`:

```json
{
  "pages": {
    "42": [
      { "key": "2:255", "start": 102.48 },
      { "key": "2:256", "start": 181.82 }
    ]
  }
}
```

- `key` = `"{سورة}:{آية}"`
- `start` = ثوانٍ من **مبدأ ملف ذلك الوجه**، لا من مبدأ السورة ولا المصحف
- الترتيب داخل الوجه تصاعديّ دائمًا (محقَّق: 0 خللٍ في 535 وجهًا)

### القاعدتان اللتان تحلّان كل شيء

```
نهاية الآية = مبدأ التي تليها في الوجه نفسه
آخرُ آيةٍ في الوجه = تنتهي بانتهاء الملف
```

### مثالٌ حقيقي — آية الكرسي (2:255)

```
الوجه   42
المبدأ  102.48s
النهاية 181.82s   (= مبدأ 2:256)
الرابط  https://ibrahimquran.com/quran/khatma/42.mp3
```

### التنفيذ

```ts
type Timings = { pages: Record<string, { key: string; start: number }[]> };

const BASE = 'https://ibrahimquran.com/quran/';

/** موضع الآية: ملفُها ومبدؤها ونهايتُها. null إن لم تُحاذَ بعد. */
function locate(t: Timings, surah: number, ayah: number) {
  const key = `${surah}:${ayah}`;
  for (const [page, rows] of Object.entries(t.pages)) {
    const i = rows.findIndex(r => r.key === key);
    if (i < 0) continue;
    return {
      url: `${BASE}khatma/${page}.mp3`,
      start: rows[i].start,
      // null تعني «إلى آخر الملف» — لا تستبدلها برقمٍ مخترَع.
      end: i + 1 < rows.length ? rows[i + 1].start : null,
    };
  }
  return null;
}
```

> ابنِ فهرسًا `Map<"s:a", …>` مرّةً عند الإقلاع. المرور على 535 وجهًا لكل
> آيةٍ يعمل، ولا يليق بشيءٍ يُستدعى مع كل تحديث تشغيل.

### التظليل أثناء التشغيل

```ts
/** الآية الجارية من موضع التشغيل: آخرُ آيةٍ مبدؤها ≤ الزمن. */
function ayahAt(rows: { start: number }[], now: number): number {
  let lo = 0, hi = rows.length - 1, at = -1;
  while (lo <= hi) {
    const mid = (lo + hi) >> 1;
    if (rows[mid].start <= now) { at = mid; lo = mid + 1; } else hi = mid - 1;
  }
  return at;
}
```

**بحثٌ ثنائيّ لا خطّيّ:** هذا يُستدعى عدّة مرّاتٍ في الثانية طوال التلاوة.

### تكرار آيةٍ بعينها

```ts
// عند كل timeupdate:
if (now >= end ?? el.duration) {
  if (++done < times) el.currentTime = start;   // أعِدها
  else { done = 0; unpin(); }                   // حرّرها وانسَب إلى ما بعدها
}
```

**فخٌّ وقعنا فيه:** آخرُ آيةٍ في الوجه نهايتُها نهايةُ الملف، فالشرط أعلاه
لا يتحقّق قبل أن يُطلق المتصفّح `ended`. فتُعالَج إعادتُها في `ended` —
وهناك **يلزم ضبطُ `currentTime` صراحةً** قبل `play()`: العنصر منتهٍ، و
`play()` وحده يرجع به إلى أول الملف بنصّ المواصفة. ثم يصل `ended` مرّةً
أخرى بعد الإرجاع فيُحسَب تكرارًا لم يُسمَع — يُهمَل بمقارنة الزمن بالمدّة.

---

## ٣. الطريق (ب): ملفٌ لكل آية

```bash
brew install ffmpeg      # أو apt install ffmpeg

rsync -avz -e "ssh -p PORT" USER@HOST:~/…/quran/khatma/ ./khatma/

python3 tools/split_ayahs.py data/ayah-timings.json ./khatma --dry-run
python3 tools/split_ayahs.py data/ayah-timings.json ./khatma -o ./ayah-audio
```

المخرجات بالتسميتين الشائعتين، كلٌّ في مجلده:

```
ayah-audio/by-ayah/001001.mp3    كما تنشرها everyayah
ayah-audio/by-id/1.mp3           بالترتيب بين الـ6236 (islamic.network)
```

الثاني وصلةٌ صلبةٌ إلى الأول لا ترميزٌ ثانٍ، فالحجم حجمُ نسخةٍ واحدة.

> **القصّ آمن:** لا آية واحدة تمتدّ عبر وجهين — مُتحقَّقٌ منه في الـ604
> ملفات. فكلُّ آيةٍ داخل تسجيلٍ واحد، ولا يلزم وصلٌ ولا لصق.

---

## ٤. ما يجب أن يتعامل معه كودك

### (أ) آيةٌ بلا توقيت

**5459 آيةً من 6236 موقَّتة** الآن. والباقي ليس صوتُه مفقودًا — بل لم
يُحاذَ بعد. `locate()` يرجع `null`.

```
✅ عطّل «شغّل هذه الآية» واترك الوجه يُتلى كاملًا بلا تظليل
❌ لا تُقدّر مبدأً بالقسمة على عدد الآيات
```

تقديرُ الموضع يضع الضوء على آيةٍ غير التي تُتلى، فيحفظ الحافظُ خطأً.
والسكوتُ أهونُ.

### (ب) وجهان غائبان من `khatma/`

`504` و`566` ليسا في ذلك التنظيم، **وهما موجودان** في الآخر:

```
khatma/504.mp3  →  pages/46 Page 3.mp3
khatma/566.mp3  →  pages/68 Page 3.mp3
```

عالِج 404 بالرجوع إلى `data/page-map.csv`، أو انسخهما تحت الاسم الناقص
مرّةً واحدةً على خادمك وانسَ الأمر.

### (ج) الصفحة التي تجتمع فيها سورتان

54 صفحةً كذلك، ولهذا كانت ملفات `pages/` **668** لا 604. الخريطة في
`data/page-map.csv`.

### (د) آخرُ صفحةٍ لكل سورة

**لا تحسبها** من أوائل الصفحات هكذا:

```
❌ آخر السورة = أول التي تليها − 1
```

أوائلُ الصفحات لا تفرّق بين صفحةٍ مشتركةٍ وصفحةٍ جديدة، والحسابُ يخطئ في
**49 سورةً من 114** — فيقول إن الفاتحة وجهان وإن البقرة تبلغ صفحة آل
عمران. استعمل `data/page-map.csv`.

### (هـ) الشبكة

- الأسماء فيها مسافات ⇒ رمِّزها.
- الخادم يدعم `Range` ⇒ لا تنزّل الملفَّ كلَّه للقفز.
- ملف الوجه ~6 ميغابايت ⇒ على شبكةٍ بطيئة، **هيّئ الملف عند فتح الصفحة لا
  عند لمس زرّ التشغيل**: `preload="metadata"` يفتح الاتصال ويجلب الترويسة
  بلا تنزيل. وأظهر حالة الانتظار — زرٌّ لا يتغيّر بعد اللمس يُقرأ عطلًا
  فيُلمَس ثانيةً فيُلغى ما بدأ.

---

## ٥. الملفات وما فيها

| المسار | الشكل |
|---|---|
| `data/ayah-timings.json` | `{ pages: { "42": [{ key, start }] } }` |
| `data/segments.csv` | `surah,ayah,mushaf_page,audio_file,start_ms,end_ms` |
| `data/segments.json` | نفسها + وصفُ المصدر |
| `data/page-map.csv` | `mushaf_page,surah,page_in_surah,khatma_file,pages_file` |

`end_ms` فارغةٌ لآخر آيةٍ في كل وجه — نهايتُها نهايةُ الملف، والمُدَد غير
مسجّلة. وُلّدها بـ`--durations` إن لزمتك.

`segments.*` **مولَّدةٌ** من `ayah-timings.json`، فلا تُحرَّر يدويًّا:

```bash
python3 tools/export_recitation.py data/ayah-timings.json -o data/
```

---

## ٦. قائمة تحقُّق قبل الإطلاق

- [ ] الفاتحة تُتلى كاملةً والتظليل يتبع آياتها السبع
- [ ] آية الكرسي تبدأ عند 102.48s من `khatma/42.mp3`
- [ ] آخرُ آيةٍ في وجهٍ ما تُتلى إلى نهايتها ولا تُبتَر
- [ ] تكرارُ آخرِ آيةٍ في وجهٍ يعمل عددَ المرّات المطلوب فعلًا (انظر ٢)
- [ ] آيةٌ بلا توقيت تُعطَّل ولا تُقدَّر
- [ ] الوجهان 504 و566 يُتلَيان
- [ ] الأسماء ذات المسافات تُرمَّز
- [ ] التغطية معروضةٌ للمستخدم أو معلومةٌ لفريقك

---

## ٧. حدودٌ تُعرَف قبل البدء

| | |
|---|---|
| التغطية | 5459/6236 آية · 535/604 وجهًا |
| سورٌ بلا أي آية | 90، 91، 99، 103، 104، 105 |
| وجهٌ ضعيف المحاذاة | 268 — يُسمَع قبل الاعتماد عليه |
| توقيتاتٌ لملفات السور | **غير متاحة** — انظر `PROMPT.md` §4(أ) |

التغطيةُ تزيد بمحاذاة ما بقي؛ والمشتقّات كلُّها تُعاد بأمرٍ واحد.
