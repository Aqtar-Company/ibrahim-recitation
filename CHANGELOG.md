# سجلّ التغيير

الأرقام على [semver](https://semver.org/lang/ar/): الأوّلُ يرتفع حين يتغيّر
معنى البيانات على من أخذها، والثاني حين يُضاف ما لا يكسر، والثالث للتصحيح.

من أخذ هذه البيانات فلينظر `version` داخل `data/segments.json` — الملفّ
يُنسَخ إلى تطبيقات أخرى ويُفارق هذا السجلّ.

---

## 1.1.0 — 2026-09-21

### أُضيف

- **`end_source` في كل مقطع.** يقول من أين جاءت نهايتُه: `silence` مقيسٌ
  داخل السكتة بين الآيتين، و`next_start` تقديرٌ هو مبدأ الآية التالية،
  و`file_end` نهايةُ ملف الوجه. اليوم **كلُّ الـ4924 حدًّا `next_start`**
  — أي كلُّها تقديرات. اقرأ العيب أدناه.
- **`tools/snap_cuts.py`.** ينقل حدود الآيات إلى السكتة الحقيقية بينها
  بمسح الصوت بـffmpeg. يُشغَّل مرّةً على الصوت فتصير الحدود `silence`.
- **`tools/fingerprint_audio.py`.** يربط التوقيتات بالصوت الذي قيست عليه،
  فيُكشَف تعديلُ الصوت بدل أن يُفسد التوقيتات صامتًا.
- `VERSION` و`CHANGELOG.md` ورابطٌ حيّ للبيانات — انظر README.
- **`tools/build_manifest.py` والفهرس الحيّ.** ملفٌّ واحد على الخادم
  (`https://ibrahimquran.com/quran/manifest.json`) يقرؤه كلُّ تطبيقٍ عند
  إقلاعه، فيه رابطُ كل ملفِ صوتٍ ومعه بصمتُه، ورابطُ التوقيتات ونسختُها.
  فإن حُسِّنت التلاوة تغيّرت بصمةُ ذلك الوجه وحده، فتغيّر رابطُه فجلبه
  المتصفّح — **ويصل التحسين إلى كل من يقرأ الفهرس**، والصوتُ وما يصفه
  ينتقلان معًا.
- **`server/.htaccess`.** الفهرس لا يُخزَّن (هو الذي يُعلِم بالتغيير)،
  والصوتُ يُخزَّن سنةً (رابطه يحمل بصمته). وبدون هذا لا يصل تحسينٌ إلى
  أحدٍ حتى تنتهي مدّةٌ لا تعرفها.
- **`LICENSE`.** التلاوة والبيانات والتوثيق بـCC BY 4.0 — لأي غرضٍ بما
  فيه التجاري، بشرط نسب التلاوة إلى د. إبراهيم حسن. والأدوات بـMIT.
  وبغير ملف ترخيص كانت كلُّ الحقوق محفوظةً افتراضًا، فلا يحلّ لأحدٍ
  استعمالُها وإن نزّلها.

### صُحِّح

- **التوثيق كان يعلّم قاعدةً خاطئة:** «نهاية الآية = مبدأ التي تليها».
  وهي التي أوقعت أوّل من دمج هذه التلاوة في تكرار أوّل حرفٍ من الآية
  مرّتين. صار التوثيق يشرح السبب ويعطي القاعدة الصحيحة.
- `export_recitation.py` كان يكتب اسم القارئ خطأً («إبراهيم حسن مطاولي»)،
  فكان أيُّ توليدٍ جديد يُعيد الاسم الخطأ. الاسم **د. إبراهيم حسن**.

### عيبٌ معلومٌ باقٍ

**حدود الآيات كلُّها تقديرات** (`end_source: next_start`). وخطأ التقدير
أعشارُ ثانية، وأكثرُه تأخّر — فيبقى في ذيل الآية أوّلُ حرفٍ من التي
بعدها، فيُسمَع مرّتين عند من يقصّ أو يقف عند النهاية. وأظهرُ ما يكون في
واو العطف وفائه وأوائل السور.

ولا يُحَسّ هذا في تطبيق المصحف نفسه: الرقم هناك يحرّك تظليلًا والصوت
متّصلٌ لا يُقَصّ.

العلاجُ يحتاج الصوت، وهو ليس في هذا المستودع:

```bash
python3 tools/snap_cuts.py data/ayah-timings.json ./khatma \
        -o data/ayah-timings.json --report cuts-report.csv
python3 tools/export_recitation.py data/ayah-timings.json -o data/
```

## 1.0.0 — 2026-09-21

أوّل نشر: توقيتات 5459 آيةً في 535 وجهًا، وأدوات التوليد والقصّ.
**هذه هي النسخة التي فيها العيب أعلاه بلا بيانٍ عنه** — من أخذها فليقرأ
`INTEGRATION.md` من جديد.
