# موجز المشروع — لمن يُكمل العمل

مكتوبٌ ليُقرأ كاملًا قبل أول تعديل، إنسانًا كان القارئ أو وكيلًا. كل رقمٍ
فيه مقيسٌ لا مقدَّر، وكلُّ ما لم يُقَس مكتوبٌ أنه لم يُقَس.

---

## ١. ما هذا

تلاوة **الشيخ إبراهيم حسن مطاولي** للمصحف كاملًا، ومعها توقيتُ كل آية،
لتُدمج في تطبيق مصحفٍ آخر — والوجهة المقصودة **تطبيق طريق**.

المصحف المستعمَل في تطبيق إبراهيم قرآن مأخوذٌ أصلًا من طريق، فترقيم
الصفحات والآيات متوافقٌ بينهما بلا تحويل. *(هذا ما أفاده صاحب المشروع؛
لم يُتحقَّق منه بمقارنةٍ آليّة.)*

المستودع الأصلي للتطبيق:
[Aqtar-Company/IbrahimQuran](https://github.com/Aqtar-Company/IbrahimQuran).
أدوات المحاذاة هناك في `tools/align_page.py`.

---

## ٢. الصوت: أين هو وكيف نُظّم

على خادم `ibrahimquran.com` تحت `public_html/quran/`، بثلاثة تنظيماتٍ
**لتسجيلٍ واحد**:

| المجلد | العدد | التسمية |
|---|---|---|
| `khatma/` | 604 (ناقصٌ ملفان) | `1.mp3` … `604.mp3` بترقيم المصحف |
| `surah/` | 114 | `2 Al-Baqarah.mp3` |
| `pages/` | 668 | `2 Page 13.mp3` — الوجه داخل سورته |

**الصوت ليس في هذا المستودع** ولا ينبغي أن يُرفع إليه: حجمه بالغيغابايت،
و`.gitignore` يستبعد كل `.mp3`.

### لماذا 668 لا 604

الصفحة التي تجتمع فيها سورتان تُعَدّ مرّتين. والعدد مُتحقَّقٌ منه: أزواج
(سورة، وجه) المشتقّة من بيانات المصحف الـ604 = **668 بالضبط**، وهو عدد
الملفات على الخادم، بلا فرقٍ واحد. الخريطة الكاملة في `data/page-map.csv`.

---

## ٣. حقائق مُتحقَّقٌ منها — يُبنى عليها

### (أ) لا آية واحدة تمتدّ عبر وجهين

فُحصت بيانات الأوجه الـ604: ليس فيها آيةٌ لها كلماتٌ في صفحتين. أطولُ آية
(البقرة 282) تملأ الوجه 48 وحدها، و49 يبدأ بـ283.

**لماذا يهمّ:** استخراج صوت الآية **قصٌّ لا تركيب**. لو كان العكس لخرجت كل
آيةٍ على حدّ صفحةٍ مبتورةً ولم يُنبّه إلى ذلك شيء.

### (ب) الملفان الغائبان من khatma موجودان في pages

| الوجه | الغائب | الموجود |
|---|---|---|
| 504 | `khatma/504.mp3` | `pages/46 Page 3.mp3` |
| 566 | `khatma/566.mp3` | `pages/68 Page 3.mp3` |

فالمصحف **كاملٌ صوتًا**؛ النقص في تنظيمٍ واحد لا في التسجيل.

### (ج) ترقيم الصفحات في التطبيق صحيح

قورن `SURAH_FIRST_PAGES` ببيانات الأوجه الـ604: صفر اختلاف. أما
`SURAH_LAST_PAGES` فكان يُحسَب حسابًا خاطئًا يزيد صفحةً في **49 سورة من
114**، وقد استُبدل بجدولٍ مشتقٍّ من البيانات نفسها.

---

## ٤. ما لم يُتحقَّق منه — يُفحَص قبل البناء عليه

### (أ) هل ملف السورة هو أوجهُها موصولة؟

التوقيتات كلها مقيسةٌ على `khatma/*.mp3`. وردُّها إلى ملف السورة يفترض أن
ملف السورة أوجهُها بترتيبها بلا زيادة — فيكون مبدأ الوجه مجموعَ مُدَد ما
قبله. **لم يُختبَر.** وقد يكون تسجيلًا آخر، أو فيه استعاذةٌ أو صمتٌ ليس في
ملفات الأوجه، فتزيح التوقيتات كلها.

**الفحص (دقيقتان):** خُذ سورةً قصيرة، واحسب مبدأ آيةٍ في وسطها بالجمع، ثم
اسمع ملف السورة عند ذلك الموضع. إن وافق صحّ الافتراض؛ وإن زاغ فالصواب
إعادةُ المحاذاة على ملفات السور نفسها.

### (ب) صيغة طريق المطلوبة للرفع

لم تُراجَع. `qul.tarteel.ai` و`everyayah.com` و`api.quran.com` كانت كلها
محجوبةً عن بيئة العمل. ما هنا صيغةٌ عامة واضحة؛ فإن كان لطريق شكلٌ محدّد
فليُطابَق عليه.

### (ج) الوجه 268

محاذاته ضعيفة الثقة بالنموذجين معًا. توقيتاتُه موجودةٌ في البيانات.
يُسمَع الملف ويُتأكَّد أنه تلاوةُ هذا الوجه فعلًا (سورة النحل، بُعيد 16:6).

### (د) 233 آيةً مبدؤها 0.0 بالضبط

من 535 صفحة، أولُ آيةٍ في 233 منها مبدؤها صفر. قد يكون طبيعيًّا (يبدأ
القارئ فورًا)، وقد يكون أثرًا لملء الفجوات حين تتعذّر محاذاةُ الآية الأولى.
لم يُفرَّق بينهما — ملفات الصوت كانت محجوبةً عن بيئة الفحص. تُسمَع عيّنة.

---

## ٥. التغطية الآن

```
535 وجهًا من 604
5459 آية من 6236   (٨٧٪)
6 سور بلا أي آية: 90، 91، 99، 103، 104، 105
0 وجهٍ ترتيبُ توقيتاته مختلّ
```

`COVERAGE.txt` يحمل الأرقام وقت آخر توليد، و`MISSING.md` يعدّد الـ69 وجهًا
غير المحاذاة ويذكر لكلٍّ اسمَ ملفه في التنظيمين.

> **ملاحظة:** هذه أرقامُ نسخة المستودع من `data/ayah-timings.json` (535
> وجهًا). على خادم `app.ibrahimquran.com` نسخةٌ أحدث فيها **597 وجهًا** لم
> تصل هذا المستودع بعد. إحلالُها يرفع التغطية إلى نحو 6100 آية.
>
> ```bash
> scp -P PORT USER@HOST:~/domains/ibrahimquran.com/public_html/app/ayah-timings.json .
> ```

---

## ٦. العمل المتبقّي، مرتّبًا

1. **إحلال ملف التوقيتات ذي الـ597 وجهًا** وإعادة توليد المشتقّات:
   ```bash
   python3 tools/export_recitation.py data/ayah-timings.json -o data/
   python3 tools/export_recitation.py data/ayah-timings.json --report > COVERAGE.txt
   ```
2. **محاذاة الـ69 وجهًا الباقية** — أداتُها في المستودع الأصلي.
3. **الوجهان 504 و566** — يُحاذَيان من `pages/` كما في القسم ٣(ب).
4. **فحص افتراض ملف السورة** — القسم ٤(أ).
5. **قصّ ملفٍ لكل آية** بعد اكتمال التغطية:
   ```bash
   python3 tools/split_ayahs.py data/ayah-timings.json ./khatma --dry-run
   python3 tools/split_ayahs.py data/ayah-timings.json ./khatma -o ./ayah-audio
   ```
6. **مطابقة صيغة طريق** — القسم ٤(ب).

---

## ٧. قواعدُ العمل هنا

- **لا يُخمَّن توقيت.** آيةٌ بلا مبدأٍ مقيس تغيب من المخرجات ولا تُقدَّر.
  توقيتٌ مزاحٌ يضع الضوء على آيةٍ غير التي تُتلى يُعلّم الحافظَ خطأً،
  وسكوتُ صفحةٍ أهونُ منه. هذا سببُ رفضِ المحاذاة صفحةً كاملةً حين تضعف.
- **يُقاس ولا يُفترض.** كلُّ رقمٍ في هذا الملف جاء من فحصٍ على البيانات:
  عدد الأوجه، وامتدادُ الآيات، وخريطةُ التسمية، وجدولُ آخر صفحةٍ لكل سورة.
  وكلُّ ما لم يُفحَص مكتوبٌ في القسم ٤.
- **تُذكَر التغطية دائمًا.** أيُّ تسليمٍ يُرفَق به ما يُغطَّى وما لا يُغطَّى.
- **هذا المستودع علنيّ.** لا يُكتَب فيه اسمُ مستخدمٍ ولا مضيفٌ ولا منفذٌ ولا
  كلمةُ مرور. العناوين في الأوامر مواضعُ `USER@HOST` و`PORT` تُملأ عند
  التشغيل.
- **لا يُرفَع صوت.** `.gitignore` يستبعد `*.mp3`.

---

## ٨. مراجع سريعة

| | |
|---|---|
| شكل التوقيتات ومصدرها | `README.md` |
| كل نقصٍ وأين بديلُه | `MISSING.md` |
| خريطة الوجه ↔ السورة ↔ التسميتين | `data/page-map.csv` |
| المقاطع للتسليم | `data/segments.csv` · `data/segments.json` |
| تحذيرُ ملفات السور | آخر `tools/export_recitation.py` |
| صيغة التسمية المعيارية | أول `tools/split_ayahs.py` |
